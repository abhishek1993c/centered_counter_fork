{
  "name": "interview_counter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "prettier": "npx prettier --write './src/**/*.ts'"
  },
  "devDependencies": {
    "prettier": "^3.3.2",
    "typescript": "^5.2.2",
    "vite": "^5.0.8",
    "vitest": "^1.2.1"
  }
}
